{
  "name": "codex-usage-tracker",
  "version": "0.1.0",
  "description": "Stream Deck plugin that displays Codex 5-hour and weekly usage windows.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run typecheck && node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "validate": "npm run build && streamdeck validate com.codexusage.tracker.sdPlugin --no-update-check",
    "pack": "npm run build && streamdeck pack com.codexusage.tracker.sdPlugin -f -o dist --no-update-check",
    "smoke": "node scripts/smoke-codex.mjs",
    "clean": "rimraf dist logs com.codexusage.tracker.sdPlugin/logs com.codexusage.tracker.sdPlugin/bin/plugin.js com.codexusage.tracker.sdPlugin/bin/plugin.js.map com.codexusage.tracker.sdPlugin/bin/package.json"
  },
  "keywords": [
    "streamdeck",
    "codex",
    "usage",
    "plugin"
  ],
  "author": "Codex Usage Tracker",
  "license": "MIT",
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0"
  },
  "devDependencies": {
    "@elgato/cli": "^1.7.4",
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
